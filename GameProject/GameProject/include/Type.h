#ifndef TYPE_H
#define TYPE_H

// Header file that can be used in any file to access the enum Type
enum Type
{
	traversable = 0, wall = 1, enemy = 2, player = 3, treasure = 4, arrows = 5
};

#endif // TYPE_H
